import { TreeNode } from "../Classes/TreeNode";
export class BuildTree {
    public buildCenterTree(vals: Array<number>): TreeNode {
        let root: TreeNode = new TreeNode({val: vals[0]});
        let repeat: number = 2;
        let index: number = 1
        while(index < vals.length) {
            //left


        }

        return root;
    }
}